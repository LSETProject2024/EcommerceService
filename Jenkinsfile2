node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    def mvn = tool 'Ecomm';
    withSonarQubeEnv('sq1') {
      sh '.mvn clean verify sonar:sonar -Dsonar.projectKey=ecommerce'
    }
  }
}
